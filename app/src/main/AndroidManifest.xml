<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        TIMING STRATEGY:
        - Core ticks every 1s with elapsedRealtime() correction to drive state/UI (see TIMING_STRATEGY_EVALUATION.md).
        - Added September 25, 2025: schedule AlarmManager exact alarms (with allowWhileIdle) to start T-10 haptics when permission granted;
          falls back to tick-only cadence if exact alarms are unavailable.
        Foreground service provides sufficient priority without manual wakelocks.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
        Android 16 (Wear OS 6 / One UI 8+) Live Updates / Promoted Ongoing Notification.
        Required when calling Notification.Builder#setRequestPromotedOngoing(true) so the
        notification is treated as a Live Update by the system: cap on watch face / Now bar /
        AOD with our own background colour and segmented progress bar (no Samsung whitelist).
    -->
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:name=".SermonTimerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">
        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />
        <uses-library
            android:name="wear-sdk"
            android:required="false" />

        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <!--
            Samsung One UI 7+ Live Notifications / Now bar opt-in. Samsung gates the full
            chipBgColor / progressSegments / nowbarPrimaryInfo extras behind a private package
            whitelist, but flagging this meta-data lets Wear-side Now bar pick up *some* of
            the extras we send via Notification.addExtras(...) when it's feeling generous.
            Documented at https://akexorcist.dev/live-notifications-and-now-bar-in-samsung-one-ui-7-as-developer-en/
        -->
        <meta-data
            android:name="com.samsung.android.support.ongoing_activity"
            android:value="true" />

        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@style/MainActivityTheme.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".tile.TileActionActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:finishOnTaskLaunch="false"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <service
            android:name=".service.TimerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <!-- Describe the special-use foreground service subtype for transparency (Android 14+) -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="timer-countdown-haptics" />
        </service>

        <service
            android:name=".tile.SermonTileService"
            android:label="@string/tile_description"
            android:description="@string/tile_description"
            android:exported="true"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@drawable/ic_launcher_foreground" />
        </service>

        <!--
            Watch-face complication: lets the user pin a live timer countdown to any
            complication slot on their watch face. Updates auto-tick on the watch face
            via TimeDifferenceComplicationText; we only push pulls on state events.
        -->
        <service
            android:name=".complication.TimerComplicationService"
            android:exported="true"
            android:icon="@drawable/ic_timer_ongoing"
            android:label="@string/complication_label"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT,LONG_TEXT,RANGED_VALUE,MONOCHROMATIC_IMAGE" />
            <!-- 0 = no automatic polling; we push updates on state changes. -->
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="0" />
        </service>

    </application>

</manifest>
