<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        TIMING STRATEGY:
        - Core ticks every 1s with elapsedRealtime() correction to drive state/UI (see TIMING_STRATEGY_EVALUATION.md).
        - AlarmManager exact alarms (USE_EXACT_ALARM) drive T-10 countdown haptics; reliable through Doze.
        - WAKE_LOCK held for the duration of an active session (PREROLL/RUNNING/OVERTIME):
          FGS exempts us from Doze background limits but does NOT keep the CPU from suspending
          between events. For 1-2 h sermons that means the 1 s tick coroutine and the Now bar
          chip would lag if we relied on FGS alone. Released as soon as the engine returns to
          IDLE / DONE so we don't drain battery between sessions.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!--
        REQUEST_IGNORE_BATTERY_OPTIMIZATIONS lets us prompt the user once to whitelist
        the app from Doze. Helps but does NOT fully neutralise Samsung Freecess (a
        separate, more aggressive layer on Galaxy Watch). The bigger persistence fix
        is the BroadcastReceiver-mediated alarm path that re-promotes FGS after kill;
        battery whitelist is defense-in-depth that simply lowers kill frequency.
    -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:name=".SermonTimerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">
        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />
        <uses-library
            android:name="wear-sdk"
            android:required="false" />

        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@style/MainActivityTheme.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".tile.TileActionActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:finishOnTaskLaunch="false"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <service
            android:name=".service.TimerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <!-- Describe the special-use foreground service subtype for transparency (Android 14+) -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="timer-countdown-haptics" />
        </service>

        <!--
            BroadcastReceiver-mediated alarm triggers. Required because
            PendingIntent.getService directly does NOT inherit the FGS-from-alarm
            exemption on Android 12+; the service-start has to be routed through a
            BroadcastReceiver.onReceive that calls startForegroundService(...).
            Both receivers are exported=false: only the system AlarmManager fires
            them via PendingIntent, no other app should be able to deliver intents.
        -->
        <receiver
            android:name=".service.BoundaryTickReceiver"
            android:exported="false" />
        <receiver
            android:name=".service.CountdownAlarmReceiver"
            android:exported="false" />

        <service
            android:name=".tile.SermonTileService"
            android:label="@string/tile_description"
            android:description="@string/tile_description"
            android:exported="true"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@drawable/ic_launcher_foreground" />
        </service>

        <!--
            Watch-face complication: lets the user pin a live timer countdown to any
            complication slot on their watch face. Updates auto-tick on the watch face
            via TimeDifferenceComplicationText; we only push pulls on state events.
        -->
        <service
            android:name=".complication.TimerComplicationService"
            android:exported="true"
            android:icon="@drawable/ic_timer_ongoing"
            android:label="@string/complication_label"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT,LONG_TEXT,RANGED_VALUE,MONOCHROMATIC_IMAGE" />
            <!-- 0 = no automatic polling; we push updates on state changes. -->
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="0" />
        </service>

    </application>

</manifest>
